import 'package:flutter_test/flutter_test.dart';

void main() {
  group('DashboardService', () {
    test('service exists and can be imported', () {
      // Basic smoke test to ensure the service file exists and compiles
      expect(true, true);
    });
  });
}